import { environment } from 'src/environments/environment';

export const PATHS = {
  albums: environment.base_url + 'albums',
  photos: environment.base_url + 'photos',
  users: environment.base_url + 'users',
};